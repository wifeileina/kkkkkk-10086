# 小卡解析 · 更新日志

> 本项目基于 ikenxuan 的「kkkkkk-10086」二次开发（Yunzai 版）。
> 原项目地址：https://github.com/ikenxuan/kkkkkk-10086
> 原作者：ikenxuan　交流群：795874649　开源许可：GPL-3.0-only
> 上游更新日志不再随本分支展示，以下仅记录本分支的改动。

## [2.36.0] 小卡解析二次开发版

### 新增功能

* 清晰度档位选择：xk解析档位 / #解析档位 / xk档位
  发送含链接的指令或引用链接消息，列出抖音、哔哩哔哩全部可用档位及其体积，
  回复序号即可下载指定档，档位列表 120 秒内有效。
* 命令前缀统一为 xk（xk解析 / xk弹幕解析 / xk解析档位 等），不再使用 kkk 前缀。
* 私聊解析独立开关（app.privateTool）：与总开关解耦，关闭后私聊静默跳过，群聊不受影响。
* 手动解析模式（manualParseEnabled / manualParseGroups）：
  指定群仅响应显式解析指令，不再自动解析裸链接。
* 缓存保留时长（app.cacheRetentionMinutes，默认 10 分钟），需先开启删除视频缓存开关。
* 解析结果表情反馈：成功贴成功表情；解析失败改贴「哭泣」表情；
  体积超限等规则限制导致的停止解析不计为失败，保持原成功表情。
* 新增 xk解析帮助 帮助页与 xk解析版本 更新日志页。

### 修复与优化

* 抖音解析改用 Puppeteer 无头浏览器获取作品详情，绕过纯 HTTP 请求被风控 403 拦截的问题。
* 体积优先逻辑统一：所选档位超限时自动下探到体积上限内的最高档；
  所有档位均超限时下探到最小档并保留超限提示。
* 哔哩哔哩信息卡与实际下载保持一致：指定档位下载时分辨率、编码、体积跟随所选档，
  不再出现「下载 360P 却显示 1080P」。
* 哔哩哔哩体积兜底：DASH 内联 size 缺失时用 HEAD 请求实测视频 + 音频体积。
* 并发安全：临时文件改用 UUID 命名，解析状态改为方法内局部变量，
  避免多群同时解析同一链接互相覆盖。
* 超限信息图提示条改为居中展示，文案「体积超限，继续下载请引用回复：xk解析」。
* 抖音信息图文本使用 mix-blend-mode 自动反色，深浅主题下均保证可读。
* 直播指令图片动画改为恒定不透明度，避免截图时动画尚未渲染完成。
* 渲染统一走 Puppeteer，移除 shotium 渲染后端。

### 其他

* 插件内覆写框架消息节流（loader.checkLimit），节流 key 增加群维度：
  同一用户把同一内容发到多个群时不再被静默丢弃，同群重复仍按 1 秒窗口节流。